#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# --- 轻量检查（不阻塞 push）---
echo "pre-push: quick checks (non-blocking)"

# 如果真的需要，你也可以在这里做非常快的校验，比如 ts 类型检查：
# CHANGED_TS="$(git diff --name-only --diff-filter=ACMRT @{push}...HEAD | grep -E '\.tsx?$' || true)"
# if [ -n "$CHANGED_TS" ]; then
#   echo "Typecheck on changed TS files..."
#   npm run -s typecheck || echo "typecheck failed (ignored)"
# fi

echo "pre-push finished (non-blocking)."
exit 0
