#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

[ "${HUSKY:-1}" = "0" ] && exit 0

echo "pre-push: quick checks (non-blocking)"
# 严格模式才阻塞
if [ "${HUSKY_STRICT:-0}" = "1" ]; then
  npx --no -- eslint -c eslint.config.js --max-warnings=0
  npx --no -- prettier -c .
fi
echo "pre-push finished (non-blocking)."
exit 0
