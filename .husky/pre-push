#!/usr/bin/env sh
[ "${HUSKY:-1}" = "0" ] && exit 0

echo "pre-push: quick checks (non-blocking)"
if [ "${HUSKY_STRICT:-0}" = "1" ]; then
  npx --no -- eslint -c eslint.config.js --max-warnings=0 || exit 1
  npx --no -- prettier -c . || exit 1
fi
echo "pre-push finished (non-blocking)."
