#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

set -e

# 仅当有暂存文件时才跑 lint-staged（避免 "No staged files" 报错）
if git diff --cached --name-only --diff-filter=ACMRT | grep -q .; then
  npx lint-staged
else
  echo "No staged files. Skipping lint-staged."
fi
